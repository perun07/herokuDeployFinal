<!DOCTYPE html>
<html>
  <head>
    <title>Cambridge Running Club</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <nav class="navbar navbar-expand-lg navbar-light bg-primary">

  <div class="container">
    <a class="navbar-brand btn btn-outline-light" href="/blog_posts">Home</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link btn btn-outline-light" href="/searches">Upcoming Races </a> 
        </li>
         <li class="nav-item active">
          <a class="nav-link btn btn-outline-light" href="/pages/about">About</a>
        </li>
      </ul>
      <p class="navbar-text float-left">
      <% if user_signed_in? %>
      Logged in as <strong><%= current_user.email %></strong>
      <%= link_to 'Profile', edit_user_registration_path, :class => 'navbar-link btn btn-outline-light' %> |
      <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link btn btn-outline-light'  %>
      <% else %>
      <%= link_to "Register", new_user_registration_path, :class => 'navbar-link btn btn-outline-light'  %> |
      <%= link_to "Login", new_user_session_path, :class => 'navbar-link btn btn-outline-light'  %>
      <% end %>
      </p>
    </div>
  </div>
  </nav>
  <body>
  <div class="container">
    <% if notice %>
     <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
   <p class="alert alert-danger"><%= alert %></p>
    <% end %>
    <%= yield %>
    </div>
    <footer>
  <div class="container">
    <a class="link" href="https://github.com/perun07/RailsCrudFinal">Created by Perun07</a>
  </div>
</footer>
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
  </body>
</html>
